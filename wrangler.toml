name = "secure-proxy-browser"
compatibility_date = "2024-01-01"

# Cloudflare Pages static assets configuration
[assets]
directory = "."

# Environment variables for Pages
[env.production]
vars = { ENVIRONMENT = "production" }

[env.staging]
vars = { ENVIRONMENT = "staging" }
